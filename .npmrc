# eslint-plugin-react peers top out at eslint ^9.7 (works fine under 10),
# and @vitejs/plugin-react 6 has an optional @rolldown/plugin-babel peer chain
# npm cannot resolve. Both are peer-metadata lag, not real incompatibilities.
legacy-peer-deps=true
